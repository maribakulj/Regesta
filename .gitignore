.cpcache/
.clj-kondo/.cache/
target/
out/
pom.xml
pom.xml.asc
*.jar
.nrepl-port
.lsp/.cache/
.calva/
.rebel_readline_history
.idea/
*.iml
.DS_Store

# Binaries downloaded by the SessionStart hook for the Claude Code web
# sandbox. The hook script and settings.json are tracked; the binaries are
# reproducible from the pinned versions in .claude/hooks/session-start.sh.
.claude/bin/
